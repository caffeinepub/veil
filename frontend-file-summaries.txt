{
  "frontend/src/index.css": "Global styles with calm OKLCH color system using warm off-white backgrounds, muted sage/dusty-rose accents, Lora serif + Inter sans-serif fonts, and utility classes for emotion badges and veil card components.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the design system with Lora/Inter font families, emotion color tokens, status colors, OKLCH-based color system, and subtle shadow utilities.",
  "frontend/src/App.tsx": "Main app component setting up TanStack Router with all routes (login, signup, dashboard, create, posts, community, admin) wrapped in a Layout with QueryClientProvider and Toaster.",
  "frontend/src/hooks/useQueries.ts": "Comprehensive React Query hooks for all backend operations with a strict `isNonAnonymous` guard applied to every query and mutation — any call with an anonymous or missing identity returns early (null/empty/throws) without dispatching to the actor, preventing the \"blob_of_principal: invalid principal\" RTS error.",
  "frontend/src/utils/wordCounter.ts": "Utility functions for counting words in text and checking minimum word count requirements (24 words for Confess/Happy emotion types), with a helper to determine if an emotion type requires a minimum.",
  "frontend/src/utils/subscriptionHelpers.ts": "Utility functions for subscription gating logic — checking if a user can create posts or go public, and returning region-appropriate pricing strings; fixed `Region.india` → `Region.India` to match the updated backend enum casing.",
  "frontend/src/components/Layout.tsx": "Simple layout wrapper composing Header, main content area, and Footer for consistent page structure across all routes.",
  "frontend/src/components/Header.tsx": "Sticky navigation header with Veil logo, authenticated nav links (Home, My Posts, Community, Admin for admins), user pseudonym display, and sign-out button that clears query cache and redirects to /login.",
  "frontend/src/components/Footer.tsx": "Minimal footer with dynamic copyright year, app tagline, and caffeine.ai attribution link with UTM tracking using window.location.hostname.",
  "frontend/src/pages/LoginPage.tsx": "Login landing page that triggers Internet Identity auth flow, redirects authenticated users with profiles to /dashboard or without profiles to /signup, and shows inline error messages on failure.",
  "frontend/src/pages/SignupPage.tsx": "Registration form with auth guards, pseudonym input, and India/Global region selector; invite code field and all URL parameter reading logic have been removed. Form submits with only pseudonym and region, routes to /dashboard on success, and displays inline backend errors on failure.",
  "frontend/src/pages/DashboardPage.tsx": "Main dashboard with auth/profile guards, emotion selection buttons (Confess/Broke/Happy) that navigate to /create with emotion param, subscription banner for expired users, and inline gating message blocking post creation.",
  "frontend/src/pages/PostCreationPage.tsx": "Post creation form with auth guards, emotion type selection from URL params, word count validation (24-word minimum for Confess/Happy), subscription gating, inline errors, and privacy notice below submit button.",
  "frontend/src/pages/MyPostsPage.tsx": "Personal post archive page with auth/profile guards, fetches posts via useGetMyPosts, sorts newest-first, and renders PostCard for each post.",
  "frontend/src/pages/CommunityFeedPage.tsx": "Community feed page with auth/profile guards, fetches public posts sorted newest-first, and renders PublicPostCard for each post.",
  "frontend/src/pages/AdminPage.tsx": "Admin page with auth/profile guards that checks isAdmin and renders AdminDashboard for admins or an access-denied message for non-admins.",
  "frontend/src/components/PostCard.tsx": "Owner post card with inline edit, privacy toggle, and delete; fixed `deletePost.mutateAsync` to pass `{ postId: post.id }` matching the updated hook signature.",
  "frontend/src/components/PublicPostCard.tsx": "Public feed post card that fetches the user's existing reaction, highlights the active reaction button, disables reactions for post owners and already-reacted users, and calls addReaction mutation on click.",
  "frontend/src/components/EmotionBadge.tsx": "Small reusable badge component rendering emotion type (Confess/Broke/Happy) with appropriate color classes using the EmotionType enum.",
  "frontend/src/components/SubscriptionBanner.tsx": "Calm subscription expiry banner accepting a Region prop and displaying region-appropriate pricing (₹150/month or $9/month) with amber styling and no red colors.",
  "frontend/src/components/AdminDashboard.tsx": "Admin moderation dashboard updated to include a fifth \"Admin Signup\" tab (grid-cols-5) that renders the new `AdminSignup` component alongside the existing Public Posts, Members, Invite Codes, and Post History tabs.",
  "frontend/src/components/AdminPublicPostsList.tsx": "Admin view of all public posts; fixed `deletePost.mutateAsync` call to pass `{ postId }` object instead of a bare string.",
  "frontend/src/components/AdminUserManagement.tsx": "Admin user management panel updated to import `useAdminSetSubscriptionStatus` (renamed from `useSetSubscriptionStatus`) and to pass `{ userId }` objects to `suspendUser.mutateAsync` and `unsuspendUser.mutateAsync`.",
  "frontend/src/components/AdminInviteCodes.tsx": "Admin invite code manager updated to use the renamed hooks (`useAdminGetInviteCodes`, `useAdminAddInviteCode`, `useAdminGenerateInviteCode`, `useAdminRevokeInviteCode`) and passes object arguments `{ code }` to `addCode.mutateAsync` and `revokeCode.mutateAsync`.",
  "frontend/src/components/AdminUserPostHistory.tsx": "Admin post history lookup refactored to use `useAdminGetUserPosts` as a mutation (not a query), driving the search on form submit via `mutateAsync({ userId })`, with `isSuccess`/`isError` state controlling result display; `deletePost.mutateAsync` now receives `{ postId }`.",
  "frontend/src/utils/urlParams.ts": "Utility module exporting `getInviteCodeFromParams` (reads invite code from URL/sessionStorage), `clearInviteCodeFromStorage` (clears stored invite code), and `getSecretParameter` (reads any named secret/token parameter from URL query string or sessionStorage, used by useActor.ts for admin token initialization).",
  "frontend/src/components/AdminSignup.tsx": "Admin-only form component with a pseudonym text input and India/Global radio group that calls the `useAdminRegister` mutation; fixed all `Region.india` → `Region.India` and `Region.global` → `Region.Global` to match the updated backend enum casing."
}