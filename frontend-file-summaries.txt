{
  "frontend/src/index.css": "Global styles with calm OKLCH color system; adds per-emotion-mode tonal CSS custom properties (bg, text, accent) for Happy (warm golden), Confess (muted neutral), and Broke (subdued slate) in both light and dark modes, alongside existing design tokens.",
  "frontend/tailwind.config.js": "Tailwind configuration extending the design system with Lora/Inter fonts, OKLCH-based semantic color tokens, and new per-emotion-mode tonal color tokens (emotion-happy-bg/text/accent, emotion-confess-bg/text/accent, emotion-broke-bg/text/accent) mapped to CSS variables.",
  "frontend/src/App.tsx": "Main app with TanStack Router — unchanged route structure, all protected page components handle their own auth guards internally via useAuth.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — added three new crisis protocol hooks: `useGetCrisisRiskPosts` (filters `adminGetAllPosts` by `ReviewFlag.crisisRisk`), `useSendAdminMessage` (calls `sendAdminMessage` with admin-composed content), and `useSendCrisisResourceMessage` (calls `sendCrisisResourceMessage` with the hardcoded template); also updated `useAdminRemovePost` to invalidate `crisisRiskPosts` on success.",
  "frontend/src/utils/wordCounter.ts": "Utility functions for counting words in text and checking minimum word count requirements (24 words for Confess/Happy emotion types), with a helper to determine if an emotion type requires a minimum.",
  "frontend/src/utils/subscriptionHelpers.ts": "Utility functions for subscription gating logic — checking if a user can create posts or go public, and returning region-appropriate pricing strings; fixed `Region.india` → `Region.India` to match the updated backend enum casing.",
  "frontend/src/components/Layout.tsx": "Simple layout wrapper composing Header, main content area, and Footer for consistent page structure across all routes.",
  "frontend/src/components/Header.tsx": "Sticky navigation header using useAuth instead of useInternetIdentity directly; calls logout from useAuth for sign-out and shows admin nav link based on role from useAuth.",
  "frontend/src/components/Footer.tsx": "Minimal footer with dynamic copyright year, app tagline, and caffeine.ai attribution link with UTM tracking using window.location.hostname.",
  "frontend/src/pages/LoginPage.tsx": "Email/password login form with show/hide password toggle and client-side validation; triggers Internet Identity auth on submit and redirects to /community on success, showing inline errors for failures.",
  "frontend/src/pages/SignupPage.tsx": "Registration form with four required fields (anonymous display name, email, password with show/hide toggle, invite code) plus region selector; validates all fields client-side, calls backend register with pseudonym/region/inviteCode, redirects to /login on success, and shows seat count indicator.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard page using useAuth for authentication guards instead of useInternetIdentity, with useEffect-based redirects to /login or /signup as needed.",
  "frontend/src/pages/PostCreationPage.tsx": "Post creation form with three emotion modes (Happy, Confess, Broke), per-mode guidance text banners with tonal color styling, a subtle safety footer shown only when BROKE + PUBLIC are both active, live character counter, visibility toggle with acknowledgement modals, and ESP modal after broke posts.",
  "frontend/src/pages/MyPostsPage.tsx": "Personal post archive page showing all of the user's posts (both private and public) with an updated subtitle, a visibility legend summarizing private/public counts, and PostCard components that clearly label each post's visibility status.",
  "frontend/src/pages/CommunityFeedPage.tsx": "Community feed page using useAuth for auth guards instead of useInternetIdentity, with useEffect-based redirects.",
  "frontend/src/pages/AdminPage.tsx": "Admin page using useAuth for auth and role checks, showing an access-denied screen for non-admins and rendering AdminDashboard for admin users.",
  "frontend/src/components/PostCard.tsx": "Owner post card with enhanced visibility indicators — private posts show a \"Private — visible only to you\" badge with Lock icon and descriptive footer text; public posts show a \"Public\" badge with Globe icon and note about community feed visibility; no share/copy-link/reshare buttons present.",
  "frontend/src/components/PublicPostCard.tsx": "Public feed post card displaying post content, emotion badge, text reaction buttons, and a collapsible comments section — the comment toggle button now shows only a label (\"Comments\") with no count, removing the engagement metric display per feed rules.",
  "frontend/src/components/EmotionBadge.tsx": "Small reusable badge component rendering emotion type (Confess/Broke/Happy) with appropriate color classes using the EmotionType enum.",
  "frontend/src/components/SubscriptionBanner.tsx": "Calm subscription expiry banner accepting a Region prop and displaying region-appropriate pricing (₹150/month or $9/month) with amber styling and no red colors.",
  "frontend/src/components/AdminDashboard.tsx": "Admin panel with all existing tabs plus a new first-position \"Crisis Risk\" tab (using `AdminCrisisRiskTab`) with a red badge count and a prominent crisis alert banner at the top when flagged posts exist; imports `useGetCrisisRiskPosts` and `ShieldAlert` icon for the crisis UI.",
  "frontend/src/components/AdminPublicPostsList.tsx": "Admin list of all posts sorted newest-first — replaced `post.isPrivate` with `post.visibility === Visibility.privateView` to match the updated Post type using the Visibility enum.",
  "frontend/src/components/AdminUserManagement.tsx": "Admin user management with suspend/unsuspend and subscription controls — fixed all mutations to pass `Principal.fromText(userId)` directly instead of `{ userId }` objects.",
  "frontend/src/components/AdminInviteCodes.tsx": "Admin invite code manager updated to use the renamed hooks (`useAdminGetInviteCodes`, `useAdminAddInviteCode`, `useAdminGenerateInviteCode`, `useAdminRevokeInviteCode`) and passes object arguments `{ code }` to `addCode.mutateAsync` and `revokeCode.mutateAsync`.",
  "frontend/src/components/AdminUserPostHistory.tsx": "Admin post history lookup by principal — replaced `post.isPrivate` with `post.visibility === Visibility.privateView` to match the updated Post type using the Visibility enum.",
  "frontend/src/utils/urlParams.ts": "Utility module exporting `getInviteCodeFromParams` (reads invite code from URL/sessionStorage), `clearInviteCodeFromStorage` (clears stored invite code), and `getSecretParameter` (reads any named secret/token parameter from URL query string or sessionStorage, used by useActor.ts for admin token initialization).",
  "frontend/src/components/AdminSignup.tsx": "Admin form to create a new member account — now generates an invite code via `useAdminGenerateInviteCode` first, then calls `useAdminRegister` with the generated code, removing the missing `inviteCode` property error.",
  "frontend/src/components/AdminFlaggedPosts.tsx": "Admin view of flagged posts grouped by post ID — updated to use `useAdminRemovePost` (calls `adminRemovePost`) instead of `adminDeletePost`, with toast notifications for success/error feedback and a confirmation dialog before removal.",
  "frontend/src/utils/cookies.ts": "Cookie utility module exporting setAuthCookie, getAuthCookie, and clearAuthCookie functions using the standard document.cookie API with Secure and SameSite=Strict attributes.",
  "frontend/src/hooks/useAuth.ts": "Custom auth hook wrapping useInternetIdentity to expose isAuthenticated, isInitializing, userId, role (admin/user/null), and a logout function that clears the auth cookie, clears the query cache, and navigates to /login.",
  "frontend/src/components/ProtectedRoute.tsx": "Route guard component that redirects unauthenticated users to /login using useAuth, showing a spinner while initializing.",
  "frontend/src/components/AdminRoute.tsx": "Admin-only route guard that redirects unauthenticated users to /login and shows an access-denied screen for non-admin authenticated users, using useAuth role check.",
  "frontend/src/components/EntryProtectionModal.tsx": "Non-dismissible modal displaying the entry protection message (\"Veil is a private space for honesty and reflection...\") with a single \"I Understand\" button; blocks outside-click and Escape key dismissal via Radix event prevention.",
  "frontend/src/components/PublicPostWarningModal.tsx": "Non-dismissible modal displaying the public post warning message (\"Public posts are witnessed by a small, private circle...\") with a single \"I Understand\" button; blocks outside-click and Escape key dismissal via Radix event prevention.",
  "frontend/src/components/AdminFlaggedComments.tsx": "New admin component listing all flagged comments with author pseudonym (resolved via getUserProfile), relative timestamp, associated post ID, and a delete button backed by an AlertDialog confirmation that calls `useDeleteComment`.",
  "frontend/src/components/AdminAllUsersTab.tsx": "New admin component listing all registered users with search filtering; each user row shows pseudonym, region, subscription status, and suspended badge, with action buttons for Suspend/Unsuspend, Apply 24-hr Cooldown, and Permanently Remove User (with confirmation dialog and toast feedback).",
  "frontend/src/components/AdminEmotionalAlertsTab.tsx": "New admin component displaying emotional monitoring alerts for users who have posted 5+ BROKE posts within 3 consecutive days; shows amber-styled alert cards with user pseudonym, BROKE post count, and date window, plus an info banner directing admins to the All Users tab for manual action.",
  "frontend/src/components/AdminCrisisRiskTab.tsx": "New admin tab component displaying all CRISIS_RISK-flagged posts with prominent red \"CRISIS RISK\" badges, post content, author pseudonym, and timestamp; each entry has three action buttons — \"Remove Post\" (AlertDialog confirmation → `useAdminRemovePost`), \"Message User\" (Dialog with textarea → `useSendAdminMessage`), and \"Send Crisis Resources\" (AlertDialog confirmation → `useSendCrisisResourceMessage`) — with toast feedback and a human-led-only notice on every card."
}